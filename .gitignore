# ไฟล์ที่ build ออกมา (ใหญ่ ไม่ต้องเก็บใน git — ปล่อยเป็น Release แทน)
dist/
build/
*.spec

# ของ Python
__pycache__/
*.pyc

# ข้อมูลส่วนตัวอยู่ที่ %USERPROFILE%\.robloxtoolkit อยู่แล้ว ไม่ได้อยู่ในโฟลเดอร์นี้
# แต่กันไว้เผื่อมีใครวางไว้
settings.json
status.json
cache.json
sessions.json
favorites.json
click_profiles.json
*.log
